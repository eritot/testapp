networking.k8s.io: v1
kind: Ingress
metadata:
    name: frontend-ingress
spec:
    rules:
    - http:
        paths:
        - path: /
          backend:
            serviceName: testappfronted-svc
            servicePort: 80
